# name: Deploy Supabase Edge Functions

# on:
#   push:
#     branches:
#       # - main
#     paths:
#       - 'supabase/functions/**'
#       - 'supabase/config.toml'
#   workflow_dispatch:
#     inputs:
#       function_name:
#         description: 'Deploy a specific function (leave empty for all)'
#         required: false
#         default: ''

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     env:
#       SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
#       SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

#     steps:
#       - uses: actions/checkout@v4

#       - uses: supabase/setup-cli@v1
#         with:
#           version: latest

#       - name: Deploy all functions
#         if: ${{ github.event.inputs.function_name == '' }}
#         run: |
#           supabase functions deploy --project-ref $SUPABASE_PROJECT_ID

#       - name: Deploy specific function
#         if: ${{ github.event.inputs.function_name != '' }}
#         run: |
#           supabase functions deploy ${{ github.event.inputs.function_name }} --project-ref $SUPABASE_PROJECT_ID
